library(dplyr) # wrangling
library(tidyverse)
library(magrittr) # piping %>%
library(tibble)

# ML libraries
library(rpart) # Entscheiungbaumregression
library(randomForest)
library(tidymodels)
library(caret)

# Plotting
library(ggplot2)

# reading files
library(readr)

# EDA
# library(correlationfunnel) Deaktiviert wegen VSCODE
library(skimr)
library(DataExplorer)

# imputation
library(mice)

# Recipe (Workflow)
library(recipes)

library(tictoc)

options(scipen = 999) # Entfernt Notationen wie "10e-3" und konvertiert sie in explizites Zahlenformat!
